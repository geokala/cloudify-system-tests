namespace: system_tests
manager_install_rpm_url:
  description: URL for manager install RPM.
manager_ssh_user:
  description: User with SSH access to manager.
  default: centos
platform:
  description: Which platform will provide resources for system tests.
